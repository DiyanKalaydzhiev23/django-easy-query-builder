# Django Easy Query Builder

This is a Django app that allows you to build complex queries with ease.

## Development

This project uses `pre-commit` to enforce code quality. To install the hooks, run:

```bash
pip install pre-commit
pre-commit install
```

Now, every time you commit a change, the hooks will be executed.
